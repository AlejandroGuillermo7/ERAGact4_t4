info:
  name: 03 - Iniciar Sesión (1)
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8088/api/auth/login
  body:
    type: json
    data: |-
      {
        "userName": "enriquez",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
